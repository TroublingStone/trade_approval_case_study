[project]
name = "trade-approval-core"
version = "0.1.0"
description = "Core trade approval domain logic"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trade_approval_core"]
